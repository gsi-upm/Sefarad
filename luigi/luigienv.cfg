[scheduler]
record_task_history = True
state_path = /usr/local/var/luigi-state.pickle

[task_history]
db_connection = sqlite:////usr/src/app/luigi-task-hist.db

[email]
force-send = True
prefix = [LUIGI]
receiver = ${EMAIL}
sender = luigi@gsi.dit.upm.es

[smtp]
host = ${SMTP_HOST}
port= ${SMTP_PORT}
